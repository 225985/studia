#!/usr/bin/env ruby

require "logger"
require "yaml"

CONFIG = YAML.load(File.read(File.join(File.dirname(__FILE__), "..", "hosts.yml")))

puts "csshx #{(CONFIG["workers"] << CONFIG["webapi"] << CONFIG["webapp"]).map{|e| "ubuntu@#{e}"}.join(" ")}"
